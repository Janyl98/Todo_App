buildscript {
    repositories {
        google()
        mavenCentral()
        // Add custom Maven if needed
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:2.1.0"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // Your Maven repo if needed:
        // maven { url 'https://your.custom.repo/' }
    }
}
